1) FrontController 모든 *.do 를 수신 web.xml 설정 완료(서버 재부팅)
2) 외부 URL 요청
3) FrontController init() 초기화
4) FrontController service() -> req url 가져오기 -> SubController를 map에서 꺼냄
5) SubController: 파라미터 받기
                  입력값 검증(JS대체 가능)
                  서비스 실행
                  service -> dao -> DB 처리
6) View로 이동(포워드 / 리다이렉트)